# Copy this file to jenkins/.env and replace the placeholders with the inbound
# agent secrets generated by Jenkins. Never commit the resulting .env file.
JENKINS_BUILD_AGENT_SECRET=replace-with-build-test-agent-secret
JENKINS_DOCKER_AGENT_SECRET=replace-with-docker-agent-secret
